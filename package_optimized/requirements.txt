# Core dependencies for S&P 500 Stock Analysis Lambda Function
boto3==1.40.40
requests==2.32.5
yfinance==0.2.28
numpy==1.24.3
pandas==2.0.3
lxml==5.3.0
html5lib==1.1

# Note: All dependencies are pre-installed in the dependencies/ folder
# and packaged in lambda_deploy.zip for AWS Lambda deployment
